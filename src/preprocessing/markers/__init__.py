"""Marker (coded-target) pipeline for phone preprocessing: CCT detection → triangulation → metric scale.
Made a package so the top-level orchestrator can `from markers import marker_scale / inject_markers_to_db`.
Superseded detector versions and one-off prototypes live in legacy/ (kept as history, not run)."""
